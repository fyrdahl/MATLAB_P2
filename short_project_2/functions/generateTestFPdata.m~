function TC = generateTestFPdata(nTimePts,nSlices,freqOffset,workingdir)
%%
% nTimePts: Defines the number points in the generated timecourse
%
%%
readFingerprintOffsetList
%%
TC = zeros(1,nTimePts);
M = zeros(1,nTimePts);
M0=1;
%%
sIx = 20;
sIy = 20;
testT1 = zeros(sIx,sIy);
testT1(:) = 100;
testT1(10:15,10:15) = 150;
testT1(5:10,5:10) = 200;
testT2 = testT1*0.8;

plotFlag = 1;
%%
for i = 1:nTimePts
    [M, Mxy,flipAngles,imageTimes, t0s] = SimSE_Bernstein(T1, T2, fingerprintOffsetList,freqOffset, nSlices, nTimePts)
    SimSE_Bernstein;
    M(i) = M0*
    TC(i) = M(i);
end
    

end
